Here is a Product Requirements Document (PRD) formatted specifically for an AI coding assistant like Replit Agent or Vercel v0. This PRD clearly defines what to build, how it should behave, and the expectations for the backend using Python + FastAPI and Anthropic Claude as the LLM. It is intentionally frontend-agnostic to allow flexibility.

⸻

📄 Product Requirements Document (PRD)

🧩 Project Name

Resume Customization App

⸻

🎯 Objective

Build a web application that helps job seekers tailor their resumes and cover letters to specific job descriptions using AI. The app should assist with editing, analyzing, and customizing resumes with ATS compatibility in mind, and provide AI-generated content to speed up the application process.

⸻

⚙️ Stack Requirements
	•	Backend Language: Python 3.10+
	•	Framework: FastAPI
	•	AI Integration: Anthropic Claude API
	•	Frontend: Agnostic (can be rendered via templates, HTMX, or exposed as an API)

⸻

🔧 Core Features & Functional Requirements

1. Resume Upload & Management
	•	Endpoint to upload resumes (initially only Markdown, later DOCX and PDF).
	•	Store resume content in a database.
	•	Return a unique resume ID.
	•	Endpoint to retrieve and update resume content.

2. Resume Editing
	•	Endpoint to fetch editable content by resume ID.
	•	Endpoint to update resume content (Markdown format).
	•	Keep version history for each update (basic versioning).

3. Job Description Input
	•	Endpoint to submit a job description:
	•	As raw text
	•	Or via a URL (must scrape content or fallback gracefully)
	•	Return a job description ID after storing it.
	•	Endpoint to fetch job description data.

4. ATS Compatibility Analysis
	•	Endpoint to score a resume against a job description.
	•	Return:
	•	Overall match score (0–100)
	•	List of matching and missing keywords
	•	Basic improvement suggestions (e.g., add education, quantify results)

5. AI-Powered Resume Customization
	•	Endpoint to trigger resume customization using Claude API.
	•	Inputs:
	•	Resume ID
	•	Job description ID
	•	Output:
	•	Enhanced resume content
	•	Store customized result with new ID

6. Cover Letter Generation
	•	Endpoint to generate a cover letter using Claude API.
	•	Inputs:
	•	Resume ID or customized resume ID
	•	Job description ID
	•	Output:
	•	AI-generated cover letter (editable by user)

7. Export Functionality
	•	Endpoints to download resume or cover letter in:
	•	Markdown
	•	PDF
	•	DOCX
	•	Files should have proper headers for downloading.

⸻

🛡️ Non-Functional Requirements
	•	All endpoints must be documented using OpenAPI (via FastAPI auto docs).
	•	Support CORS for external frontend access.
	•	Handle large uploads gracefully (limit to 5MB per file).
	•	Secure API with optional token-based auth (for future phases).
	•	Claude API keys must be loaded from environment variables.

⸻

🧠 AI Prompt Guidelines for Claude

Customize resumes using Claude by:
	•	Emphasizing relevant skills already present.
	•	Reordering content for clarity and impact.
	•	Highlighting achievements (especially quantified ones).
	•	Never fabricating experience or adding unverified claims.

⸻

🧪 Initial MVP Workflow
	1.	User uploads a Markdown resume.
	2.	User inputs a job description as plain text.
	3.	The app compares them and returns:
	•	ATS match score
	•	Keyword matches/mismatches
	4.	User requests AI to customize the resume.
	5.	Claude generates a tailored resume based on job description.
	6.	User can also request a generated cover letter.
	7.	User downloads final versions.

⸻

📘 Future Enhancements (out of scope for MVP)
	•	Authentication and user profiles
	•	Real-time collaborative editing
	•	Payment integration and usage tracking
	•	Resume builder UI with drag-and-drop

⸻

✅ Deliverables Checklist for AI Assistant
	•	FastAPI project with /resumes, /jobs, /ats, /customize, and /cover-letter endpoints
	•	Claude integration for customization and cover letter generation
	•	Resume/job description comparison logic
	•	Download routes for PDF, DOCX, and Markdown formats
	•	CORS and OpenAPI documentation enabled

⸻

Let me know if you’d like a follow-up PRD for frontend or deployment planning.